// Default global activity point conversion table
import { ActivityPointConversion } from '@/types';
import { DEFAULT_NORMALIZED_ACTIVITY_POINT_CONVERSION } from './activityNormalization';

// Export the normalized conversion rates
export const DEFAULT_ACTIVITY_POINT_CONVERSION: ActivityPointConversion[] = DEFAULT_NORMALIZED_ACTIVITY_POINT_CONVERSION; 